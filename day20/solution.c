/*
Welcome to Day 20 of your Vim challenge!

Today, we'll learn about the argument list.

New commands for today:
    - `:args`: Show the current argument list.
    - `:next`: Go to the next file in the argument list.
    - `:prev`: Go to the previous file in the argument list.
    - `:first`: Go to the first file in the argument list.
    - `:last`: Go to the last file in the argument list.
    - `:argadd <filename>`: Add a file to the argument list.
    - `:argdelete <filename>`: Remove a file from the argument list.

Reminder from Day 19:
    - Custom commands (`:command`).

Your tasks:
1.  Add `file2.c` and `file3.c` to the argument list.
2.  Navigate through the argument list using `:next` and `:prev`.
3.  Remove `file2.c` from the argument list.
*/

#include <stdio.h>

int main() {
    printf("Vim challenge Day 20!\n");
    return 0;
}
